<h1>Log listing page </h1>

<table>
  <thead>
    <tr>
      <th>Shift </th>
      <th>Notes</th>
      <th colspan="3">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @daily_logs.each do |log| %>
      <tr>
        <td><%= log.shift %></td>
        <td><%= log.notes %></td>
        <td><%= link_to 'Show', daily_log_path(log) %></td>
        <td><%= link_to 'Edit', edit_daily_log_path(log) %></td>
        edit_daily_log
        <td><%= link_to 'Delete', daily_log_path(log), method: :delete, data: { confirm: "Are you sure?"} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

  <p>
    <%= link_to 'Create new article', new_daily_log_path %>
  </p>


