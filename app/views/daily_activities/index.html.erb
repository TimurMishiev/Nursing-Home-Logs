<div class="container">
   <h1 class="text-center mt-4"> All Daily Activities</h1>
      <% @daily_activities.each do |activity| %>
         <div class="row justify-content-md-center">
            <div class="col-6 mt-4">
               <div class="card text-center mb-4 bg-white rounded">
                  <div class="card-body">
                     <h5 class="card-title">Daily Activity: <%= link_to activity.title, daily_activity_path(activity) %></td></h5>
                     <p><%= truncate(activity.description, length: 100) %>
                     </p>
                     <%= link_to "View",  daily_activity_path(activity), class: "btn btn-outline-dark" %>
                  </div>
               </div>
            </div>   
         </div>  
      <% end %> 
<div>
 